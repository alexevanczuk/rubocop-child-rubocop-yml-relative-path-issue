# frozen_string_literal: true

source 'https://rubygems.org'

gem 'pry-byebug'
# Change the next line to use rubocop head to fix the issue and properly trigger the cop
# gem 'rubocop', path: '../rubocop'
gem 'rubocop', '1.26.1'
